2018年2月21日16:52:35
本实验的一些体会：
    1.在调用读写函数时，配对使用。

                //将数据读入到缓冲区对象中
                len = file->read(buf, sizeof(buf));
                //将数据写入QTcpSocket
                len = tcpSocket->write(buf, len);

         在上述读取和写入操作中，读取缓冲区长度的数据到缓冲区buf中，并且获取读取的长度
     然后将buf 中的长度为len的数据写入到套接字中。
        上述的write函数不能使用如下重载形式：
            qint64 QIODevice::write(const char *data)
         因为该重载形式只能写8个字节，所以以后最好的调用读写函数的方式就是配对调用。

    2.变量的初始化，累加数最好在开始时就初始化。

                receive_size += len;

        在接收文件时，开始没有对receive_size进行初始化，程序给了默认的垃圾值，然后导致
     所有的len值都加到了一个垃圾值上，最后得到的文件大小永远都没有和原文件的大小一
     样，永远都不会显示接收完成。
